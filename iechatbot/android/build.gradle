buildscript {
    repositories {
        google() // Required for Android Gradle Plugin
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.4.2'  // or try 7.3.0
        // Add any other classpath dependencies here
    }
}

allprojects {
    repositories {
        google()  // Required for Android dependencies
        mavenCentral()
    }
}








/*allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

dependencies {
    classpath 'com.android.tools.build:gradle:7.4.2' // Adjust if necessary
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
